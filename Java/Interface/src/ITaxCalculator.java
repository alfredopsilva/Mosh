public interface ITaxCalculator {
    // We don't have any fields in Interfaces.
    // We don't need to write the data modifier type. In interfaces by default it will be Public.

   double calculateTax();
}
